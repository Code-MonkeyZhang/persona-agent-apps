[project]
name = "health-manager"
version = "0.1.0"
description = "Personal health data management system"
requires-python = ">=3.9"
dependencies = [
    "matplotlib>=3.7.0",
    "pyyaml>=6.0.0",
    "transformers",
    "torch",
    "torchvision",
    "pillow",
    "pdf2image",
    "accelerate",
]

[tool.hatch.build.targets.wheel]
packages = []
